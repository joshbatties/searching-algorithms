def dijkstra(graph, source):
    """Algorithm for solving the single source, all targets shortest path problem on graphs with non-negative weights."""
    distance, predicessor = [-1] * len(graph)
    Q = priority_queue()

    while Q:
        u = Q.pop()

def priority_queue():
    pass